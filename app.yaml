runtime: custom
env: flex
service: buff163-scraper

env_variables: 
  JWT_SECRET: $JWT_SECRET

instance_class: B1

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10